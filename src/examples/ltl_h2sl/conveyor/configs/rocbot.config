======== General Config ========

Actuator_Proposition_Mapping: # Mapping between actuator propositions and actuator handler functions
pickup_from_tray = rocbot.RocbotActionHandler.action_dispatch( action_type="ACTION_TYPE_PICKUP", objects=[ ("robot_hand_left", "na"), ("cube", "na"), ("tray", "na") ]) 
pickup_from_side = rocbot.RocbotActionHandler.action_dispatch( action_type="ACTION_TYPE_PICKUP", objects=[ ("robot_hand_left", "na"), ("cube", "na")("bin", "na") ]) 
drop_in_bin = rocbot.RocbotActionHandler.action_dispatch( action_type="ACTION_TYPE_DROP", objects=[ ("robot_hand_left", "na"), ("cube", "na"), ("bin", "na") ]) 
place_on_tray = rocbot.RocbotActionHandler.action_dispatch( action_type="ACTION_TYPE_PLACE", objects=[ ("robot_left_hand, "na"), ("cube", "na"), ("tray", "na") ]) 

Initial_Truths: # Initially true propositions

Main_Robot: # The name of the robot used for moving in this config
rocbot

Name: # Configuration name
rocbot

Region_Tags: # Mapping from tag names to region groups, for quantification
{}

Sensor_Proposition_Mapping: # Mapping between sensor propositions and sensor handler functions
tray_on_left = rocbot.RocbotSensorHandler.sensor_type_left(object_type1="OBJECT_TYPE_TRAY", object_color1="na",object_type2="OBJECT_TYPE_ROBOT_TORSO",object_color2="na")
tray_on_right = rocbot.RocbotSensorHandler.sensor_type_right(object_type1="OBJECT_TYPE_TRAY", object_color1="na",object_type2="OBJECT_TYPE_ROBOT_TORSO",object_color2="na")
tray_full = rocbot.RocbotSensorHandler.sensor_type_full(object_type="OBJECT_TYPE_TRAY", object_color="na")

======== Robot1 Config ========

CalibrationMatrix: #default, so it does not show warnings
array([[1, 0, 0],
       [0, 1, 0],
       [0, 0, 1]])

InitHandler: # Robot default init handler with default argument values
rocbot.RocbotInitHandler()

SensorHandler: # Rocbot simulator world state, triggers sensors
rocbot.RocbotSensorHandler()

DriveHandler:
share.Drive.HolonomicDriveHandler(multiplier=50.0,maxspeed=999.0)

LocomotionCommandHandler:
rocbot.RocbotLocomotionCommandHandler(speed=1.0)

MotionControlHandler:
share.MotionControl.VectorControllerHandler()

PoseHandler:
rocbot.RocbotNullPoseHandler()

RobotName: # Robot Name
rocbot

Type: # Robot type
rocbot
